<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- The accessibility service itself still needs no permission beyond the
         accessibility APIs (granted separately, in Settings): it only ever
         taps a control already visible on screen, never touches the network,
         and never reads device storage beyond the one folder the user
         explicitly picks via the system folder picker below (which grants
         its own scoped access - no broad storage permission needed for
         that). These two permissions cover only the optional (off by
         default), local-music-during-ads foreground service - see
         AdMusicPlaybackService's doc comment and docs/TODO.md for why a
         real foreground service is required, not just requested for its
         own sake. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.AppCompat.NoActionBar"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".SpotifyAdSkipService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- mediaPlayback-typed foreground service hosting the ExoPlayer instance
             for local-music-during-ads (docs/TODO.md). exported="false": only this
             app's own AdAudioController ever starts it - no other app has any
             reason to bind to or start this service. -->
        <service
            android:name=".audio.AdMusicPlaybackService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="false">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaSessionService" />
            </intent-filter>
        </service>

        <!-- Lets "Share Diagnostic Log" hand the file to another app (email, Files,
             a text editor) via a content:// Uri, without making the file world-readable -
             exported="false" plus per-request grantUriPermissions is the standard,
             narrowly-scoped way to share a single app-private file. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
