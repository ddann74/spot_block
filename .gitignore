*.iml
.gradle/
local.properties
.idea/
.DS_Store
# Unanchored (no leading slash) so this catches every module's build output,
# not just a root-level build/ folder.
build/
captures/
.externalNativeBuild/
.cxx/
